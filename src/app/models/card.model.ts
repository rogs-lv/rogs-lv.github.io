export interface card {
    text: string;
    source: string;
    maxWidth: string;
    sizeIcon?: string;
    mtSpecific?: string;
}

export interface images {
    src: string;
    style: any;
    tooltip: string;
}